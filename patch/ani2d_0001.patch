--- a/src/aniAFT/aft2d.c	2011-08-21 01:09:40.000000000 +0400
+++ b/src/aniAFT/aft2d.c	2014-09-24 18:56:10.595621100 +0400
@@ -160,6 +160,9 @@
    free(brglobal);
    free(vrbrglobal);
 
+   brglobal = NULL;
+   vrbrglobal = NULL;
+
    free(vrtglobal);
    free(bndglobal);
    free(crvglobal);
@@ -281,8 +284,8 @@
 
    free(trglobal);
    free(vrglobal);
-   free(brglobal);
-   free(vrbrglobal);
+   //free(brglobal);
+   //free(vrbrglobal);
 
    free(vrtglobal);
    free(bndglobal);
